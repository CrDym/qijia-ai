# 可选：没有 Key 也可上传原件、管理资料和家庭健康档案。
# 将此文件复制为 .env.local，在本机填写真实值，不要提交密钥。
# 也可在 /settings 的「AI 设置」中配置，页面保存值优先；环境变量作为回退。
OPENAI_API_KEY=
# 图片解析与文字整理共用此模型，需支持图片输入、Responses API 和结构化输出。
OPENAI_MODEL=gpt-4.1-mini

# 可选：默认保存到项目 data/family.sqlite；部署时请指向持久磁盘。
DATABASE_PATH=./data/family.sqlite
