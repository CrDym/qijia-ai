# 参与贡献

感谢你关注栖家。项目优先解决家庭日常中的真实需求，保持简单、模块化和可维护性。

## 提交问题与建议

请在 [Issues](https://github.com/CrDym/qijia-ai/issues) 中说明：

- 期望实现的目标，或遇到的问题。
- 可复现的操作步骤、实际结果与预期结果。
- 相关的 Node.js 版本、浏览器及运行方式。

使用虚构文字或脱敏样例。不要提交真实家庭资料、病历、数据库、密钥或包含这些内容的截图与日志。

## 开发流程

1. Fork 仓库并创建工作分支；较大的功能或架构调整请先通过 Issue 讨论。
2. 按 [README](README.md#快速开始) 安装依赖并启动应用。
3. 将修改控制在一个清晰的主题内，保留既有数据兼容性。
4. 功能、环境变量、安装方式或使用限制有变化时，同步更新 README 和 [使用指南](docs/guide.md)。README 面向首次访问项目的人，详细操作放在使用指南。
5. 运行与改动相关的检查后提交 Pull Request。

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

涉及数据写入、文件解析或 AI 输出时，补充能覆盖实际风险的测试。测试使用隔离数据库和模拟 AI 响应，避免读写开发者的真实资料。

## 提交与 Pull Request

commit 信息使用中文，简要说明具体变化。例如：

```text
新增：支持病历图片识别与原件保存
修复：切换成员后健康记录列表未刷新
文档：补充本地部署与数据备份说明
```

每次完成一组相关、已验证的修改后及时提交。向维护者仓库推送时使用 SSH，不强制覆盖远端历史。

Pull Request 说明应包含修改目的、用户可见的行为变化、验证结果，以及需要注意的兼容性或数据变更。请区分已验证的行为与尚未验证的真实 AI 效果。

## 代码约定

- 所有 AI 调用放在 `src/services/ai/`，API Key 只在服务端读取。
- 用 schema 定义输入和 AI 输出，并执行运行时校验。
- 数据访问放在 `src/repositories/`，需要一起保存的数据使用事务。
- 不为尚未确定的功能提前增加基础设施或第三方依赖。
- 不提交 `.env.local`、数据库、原始附件、构建产物或本地测试截图。

本仓库的自动化协作约定见 [AGENTS.md](AGENTS.md)。
